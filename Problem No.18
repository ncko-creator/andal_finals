int[] arr = {1, 2, 3, 4, 2};
boolean foundDup = false;

for (int i = 0; i < arr.length; i++) {
    for (int j = i + 1; j < arr.length; j++) {
        if (arr[i] == arr[j]) {
            foundDup = true;
            break;
        }
    }
}
System.out.println("Has duplicates: " + foundDup);
